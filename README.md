# A template for C projects.

A simple c project setup that uses Zig as the build system.

To compile:

```sh
$ zig build
```

To run:

```sh
$ zig build run
```

To test:

```sh
$ zig build test
```
